# =============================================================================
# INFRASTRUCTURE CONFIGURATION
# =============================================================================
# Service ports and hosts (internal Docker routing)
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0
FRONTEND_PORT=5173
FRONTEND_HOST=0.0.0.0
N8N_PORT=5678
DISCORD_BOT_PORT=8545
DOCKER_ENV=true
RESTART_POLICY=unless-stopped

# Internal service names (safe to expose)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DATABASE=0
QUEUE_BULL_REDIS_HOST=redis
QUEUE_BULL_REDIS_PORT=6379
QUEUE_BULL_REDIS_DB=0